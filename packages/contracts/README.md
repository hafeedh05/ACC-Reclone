# Contracts

This package is the source of truth for user-facing API and event contracts.

- `openapi/`: HTTP contract
- `events/`: runtime event schemas
- `src/`: small TypeScript mirrors used by the web app for mocks and typed UI state
